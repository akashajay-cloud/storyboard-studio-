// Retired: the style-picker feature was removed — panels now use a fixed photoreal cinematic look.
// (Kept as a harmless stub so a GitHub web-upload overwrites the old version, which imported helpers
//  that no longer exist. Safe to delete entirely once deploying via git.)
export default async () => new Response("gone — style feature removed", { status: 410 });
